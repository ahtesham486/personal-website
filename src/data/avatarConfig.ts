/**
 * Avatar mode toggle
 * - true  → Your photo-based custom 3D avatar (personalAvatar.ts)
 * - false → Original cartoon GLB model (character.ts) — code kept, not deleted
 */
export const USE_PERSONAL_AVATAR = true;

export const PERSONAL_FACE_URL = "/images/ahtasham-face.png";
